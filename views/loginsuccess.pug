html 
    head 
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title #{title}
        style
            include css/style.css
    body
        h1(class="mt-3 center-text") #{title} 
        p(class=" mt-3 center-text") Silahkan Kembali ke Aplikasi
        p(class="mt-3 center-text") #{user}